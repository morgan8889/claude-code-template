# Testing Rules

- Write or update automated tests for changed behavior.
- Run unit tests, typecheck, lint, build, and E2E/visual checks when UI changes.
- Treat screenshots as evidence only after confirming they are nonblank and match the requested outcome.
- Do not open a PR with failing local gates unless the failure is explicitly documented as an external blocker.
